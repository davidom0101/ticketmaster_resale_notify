# Required
TICKETMASTER_URL="https://www.ticketmaster.ie/your-event-url"
NTFY_TOPIC="your_ntfy_topic"

# Optional (with defaults)
# HEADLESS="False"  # Set to "True" to run browser in headless mode
# CHECK_INTERVAL_MIN="8.0,12.0"  # Random interval range in minutes between checks
# BROWSER_TIMEOUT="360"  # Time in seconds to keep browser open when tickets are found
# MAX_RETRIES="3"  # Maximum number of retries for failed operations
# RETRY_DELAY="5"  # Initial delay between retries in seconds (will increase with each retry)
# LOG_LEVEL="INFO"  # Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
